

* fancybox content element
	
	- simple container, class = hidden
	- wewnatrz element fancybox-ce fancybox-something

	- w rte anchor do #cXX i class/style fancybox




* uprzednio config:

RTE.classes {
	fancybox {
		name = Fancybox class
		value = text-decoration: underline;
	}
}
RTE.default {
	classesAnchor := addToList(fancybox)
	proc.allowedClasses := addToList(fancybox)
}
	(+ w rte.css)

